const menu = document.getElementById('like-menu');

function OverLikeMenu(){
    const menu = document.getElementById('like-menu').classList.add("active");
};

function OutLikeMenu(){
    const menu = document.getElementById('like-menu').classList.remove("active");
};