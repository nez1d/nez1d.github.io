const ROOT_CART = document.getElementById('cart');
const ROOT_FOOTER = document.getElementById('footer');
const ROOT_HEADER = document.getElementById('header');
const ROOT_LIKE = document.getElementById('like');
const ROOT_PRODUCTS = document.getElementById('products');
const ROOT_PURCHASES = document.getElementById('purchases');
const ROOT_USER = document.getElementById('user');
const ROOT_LOGIN = document.getElementById('login');
const ROOT_REGISTER = document.getElementById('register');
const ROOT_HELP = document.getElementById('help');
const ROOT_BUY = document.getElementById('buy');